# RAG Docs — Claude Code 가이드

## 프로젝트 개요

Docusaurus 기반 RAG Platform 문서 사이트. Diátaxis 프레임워크(Tutorial/Explanation/How-to/
Reference)를 기준으로 `docs/` 하위 구조를 잡는다 — 상세 트리와 각 카테고리의 역할은
`content-reorg-proposal.md` 참고.

**`.md` 파일은 MDX로 컴파일된다.** 주석은 HTML 주석(`<!-- -->`)이 아니라 `{/* ... */}`로
쓴다 — `<!-- -->`는 MDX 파서가 JSX로 해석을 시도하다 컴파일 에러를 내고 dev 서버 전체가
죽는다(실제 사고 이력 있음). 파일을 만들거나 고친 뒤에는 dev 서버 로그나
`npm run build`로 컴파일 에러가 없는지 확인한다.

## 문서 구조 원칙

- `intro.md`(소개) → `getting-started/`(Tutorial) → `concepts/`(Explanation) →
  `guides/<product>/`(How-to) → `reference/<product>/`(Reference) → `deployment/` →
  `support/` → `faq.md` 순서.
- `guides/`, `reference/`는 제품(rag-api / rag-admin / rag-ent)별 하위 폴더로 나눈다. 하위
  파일 구성은 **백로그 항목을 나열하지 않고, 그 제품의 실제 도메인 모델(KB/Connector/
  Pipeline/Document/Search 등)이나 실제 UI 메뉴 구조를 그대로 뼈대로 쓴다** — rag-admin
  가이드는 좌측 사이드바 메뉴 순서와 1:1 대응시킨다.
- `concepts/`는 내부 엔지니어링 문서(`docs/internal/architecture/*` 등)를 그대로 옮기지
  않는다. 대상 독자가 "코드를 고치는 사람"이 아니라 "도입·연동하는 사람"이므로, 같은 소재라도
  구현 세부사항이 아니라 그 구조가 왜 그렇게 생겼는지에 집중해서 다시 쓴다.

## Guides(How-to) 문서 구성

`guides/<product>/` 문서는 아래 세 가지를 기본 틀로 삼는다
(`guides/rag-api/kb.md`가 예시).

1. **설정 방법** — 대상에 전역 설정과 개별(엔터티별) 설정이 둘 다 있으면(예: KB), 전역
   설정(`settings.yaml` 등) → 개별 오버라이드 순서로 먼저 다룬다. 개별 설정이 전역값 위에
   얹히는 관계라는 것부터 밝히고 각각을 설명한다.
2. **기능 영역별 그룹** — 본문은 기능 영역별로 소제목(H2/H3)을 나누고, 각 영역을 **기능
   소개 한두 문장 + API 예제(curl 등 실행 가능한 형태)** 한 쌍으로 구성한다. 여러 기능을
   한 문단에 뭉쳐 쓰지 않는다.
3. **내부 흐름 설명이 필요한 경우** — 작업을 정확히 수행하는 데 필요한 구조(설정이 어떻게
   반영되는지, 처리·검증 순서 등)는 guides 안에서 직접 설명한다. `concepts/`는 더 깊은
   배경 설명이 필요할 때만 보충 링크로 연결한다 — concepts가 guides의 설명을 대체하지
   않는다.

## 제목(헤딩) 구성

- 문서(H1)당 정확히 1개. 페이지 제목을 본문 헤딩에서 그대로 반복하지 않는다.
- 섹션 제목(H2/H3)은 **완결된 문장(주장)이 아니라 명사구**로 쓴다 — "parse는 고정 단계가
  아니라 레지스트리 조회다"가 아니라 "파서 레지스트리"처럼. 주장·설명은 본문에서 하고,
  제목은 그 섹션이 다루는 대상만 짧게 가리킨다.
- 불필요한 번호 매기기를 피한다 — 절차(순서가 의미 있는 단계)에는 번호를 쓰고, 병렬적인
  주제 나열에는 번호 없이 헤딩만 쓴다. "0. 전체 구성도"처럼 0번부터 시작하는 번호는 쓰지 않는다.
- 헤딩 문구는 간결하게 — 구두점이 많아지면 헤딩이 너무 복잡하다는 신호다.

## 표 구성

- 비교 대상 속성이 3개 이상이고 항목별로 나란히 볼 필요가 있을 때만 표를 쓴다. 그보다 단순한
  나열은 리스트로 충분하다.
- **같은 대상을 비교하는 표가 여러 개로 쪼개져 있으면 하나로 합친다** — 예: "컴포넌트별 역할"
  표와 "Core/Enterprise 차이" 표가 둘 다 rag-api/rag-ent-api를 다룬다면 표 하나로 합쳐서
  중복을 없앤다.
- **셀 내용은 완결된 문장이 아니라 명사형으로 간결하게 쓴다.** "~한다", "~된다" 같은 서술형
  종결을 피하고 핵심 정보만 남긴다 (예: "큐 용도로만 좁게 쓴다" → "큐 전용").
- 표를 도입하는 문장은 완결된 문장으로 쓴다("아래 표는 ~를 비교한다" 같은 partial sentence로
  표에 문장을 끝맺게 하지 않는다).
- 헤더 행과 첫 열만 레이블 역할을 하도록 구성한다. 표를 레이아웃 용도로 쓰지 않는다.

## 문단·문장 구성

- **격식체(문어체)로 쓴다.** "~다/~한다" 종결을 쓰고, "떠 있다", "붙는다" 같은 구어체 표현
  대신 "실행된다", "연결된다" 같은 문어체 서술어를 쓴다. 대시(—)로 절을 계속 이어붙이지 말고
  문장을 끊어서 쓴다.
- 한 문장에 하나의 주장만 담는다. 여러 개념을 대시나 쉼표로 계속 이어붙이지 않는다.
- **"이 문서는", "이 절은" 같은 문서 자기지시 표현을 주어로 쓰지 않는다.** 주어를 생략하고
  바로 서술하거나("검색과 인제스트가 실제로 어떻게 동작하는지 다룬다"), 다루는 대상을
  주어로 삼는다.
- 나열이 3개 이상이면 문장으로 풀지 말고 리스트나 표로 바꾼다.
- 메커니즘 설명(등록/조회/확장/교체처럼 단계나 항목이 나뉘는 내용)이 문단 여러 개로 길게
  이어지면, 각 항목을 **굵은 라벨 + 짧은 설명**의 불릿으로 바꾼다. 문단은 "이게 뭔지"를 한
  문장으로 도입하는 역할만 하고, 나머지는 불릿이 담당한다.
- **실제 코드·설정·커밋에 근거해서 쓴다.** "일반적으로 이렇게 동작한다" 식의 일반론이 아니라,
  실제 파일 경로·함수명·설정 키를 확인하고 그 사실을 근거로 서술한다. 확인하지 못한 내용은
  추측으로 채우지 않는다.
- 링크 텍스트는 문맥 없이 읽어도 뜻이 통하게 쓴다 ("여기를 클릭" 금지, "커넥터 가이드"처럼
  목적지를 그대로 드러내는 텍스트 사용).

## 다이어그램

- 구조(컴포넌트 관계, 레이어 구성)를 다루는 문서는 **프로즈보다 다이어그램을 먼저** 보여주고,
  이어지는 절에서 다이어그램의 각 부분을 설명하는 순서로 구성한다.
- ASCII 다이어그램에서 같은 레벨의 박스는 폭을 통일한다. 레이어가 나뉘는 구조(Application/
  Data/Pipeline 같은)는 구분선과 레이어 라벨로 명시적으로 나눈다.
- **다이어그램(ASCII 아트, 코드블록 안의 흐름도)의 모든 텍스트는 예외 없이 영어로 쓴다** —
  아키텍처/컴포넌트 다이어그램뿐 아니라 처리 흐름·상태 전이·단계별 다이어그램도 동일하게
  적용한다. 본문 프로즈는 한국어로 쓰되, 다이어그램 안 라벨·화살표 설명·주석은 전부 영어다.

## 용어

- 도메인 용어(Knowledge Base/KB, Connector, Document, Search 등)는 문서 전체에서 동일한
  표기로 통일한다 — 첫 등장 시 정의하고 이후 축약형을 일관되게 쓴다(예: "지식베이스(KB)" →
  이후 "KB").
- 코드 심볼(설정 키, 함수명, 엔드포인트)은 백틱으로 감싸고 실제 코드와 대소문자·철자를
  일치시킨다.

## 참고

이 표준은 Google Developer Documentation Style Guide, Microsoft Writing Style Guide의
일반 원칙(제목/표/리스트 구성, 간결한 문장, 스캔 가능한 구조)을 rag-docs 맥락에 맞게
재구성한 것이다.
