# RAG Docs 콘텐츠 재구성 기획안

작성일: 2026-07-20
범위: `docs/overview`, `docs/install`, `docs/operations`, `docs/reference`, `docs/support`
제외: `docs/demo` (별도 재작업 예정, 이번 분석에서 제외)

## 0. 전제 — 바꾸지 않는 것

- 5개 카테고리(개요/설치/운영/레퍼런스/지원)와 `_category_.json`의 `position`, 사이드바
  자동생성(`sidebars.js`: `autogenerated, dirName: '.'`) 구조는 그대로 유지한다.
- 카테고리를 새로 만들거나 나누거나 합치지 않는다. 이번 제안은 **카테고리 내부에서 어떤
  파일에 어떤 성격의 내용이 들어가는지, 파일을 어떻게 쪼개거나 합칠지, 각 파일 내부를 어떤
  틀로 쓸지**에 한정한다.
- orkes.io는 아래 6개 페이지만 표본으로 봤다(전체 사이트 미분석): 최상위 `/content/` 인덱스,
  `/content/core-concepts`, `/content/developer-guides/building-workflows`,
  `/content/reference-docs/worker-task`, `/content/category/conceptual-guides`. 이 표본에서
  관찰된 패턴만 참고했고, orkes 고유 카테고리 이름(Getting Started/Conceptual/Developer
  Guides 등)을 rag-docs에 그대로 이식하지 않는다 — 이미 있는 5개 카테고리에 **패턴만** 흡수한다.

---

## 1. orkes.io에서 관찰한 패턴 (표본 분석)

### 1-1. 카테고리 레벨 (최상위 인덱스 1개 페이지)

| 관찰 | 내용 |
|---|---|
| 진입점 | 랜딩에서 "신규 사용자 / 이미 구축 중 / AI 워크플로우 중심" 3갈래 독자 페르소나를 먼저 제시하고, 각 갈래가 다른 문서로 연결 |
| 카테고리 성격 구분 | Getting Started(여정) / Conceptual Guides(원리) / Developer Guides(실무) / Reference Docs(룩업) — 이 넷은 **여정 단계가 아니라 콘텐츠 유형**으로 나뉨 |
| 소개 스타일 | 각 항목 "제목 + 한 줄 부연설명", 난이도·소요시간 표시는 없음 |

### 1-2. 페이지 레벨 (표본 4개)

| 페이지 유형 | 표본 | 관찰된 구조 |
|---|---|---|
| **개념 페이지** | `core-concepts` | 제목 아래 1문단 요약 → 핵심 개념 불릿 → H2(대주제 3개)/H3(하위주제) 계층 → 산문 설명 + 비유("blueprint") → 코드/다이어그램 없음 → 끝에 prev/next, on-this-page 목차 |
| **허브 페이지** | `developer-guides/building-workflows` | 카테고리의 얼굴 역할. 한 줄 도입 후 7개 하위주제를 "제목+한줄설명" 링크 카드로만 나열. 절차·코드 없음. 실제 내용은 하위 페이지로 위임. 스크롤 거의 없음 |
| **레퍼런스 페이지** | `reference-docs/worker-task` | 짧은 개념 도입 + Prerequisites → 파라미터를 범주별(Caching/Schema/Other)로 나눈 표(Parameter/Description/Required) → 코드 예제 1개 + 스크린샷 → 관련 문서 링크 6개 |
| **Conceptual Guides 소속 문서 3개** | 카테고리 페이지 | "아키텍처", "워크플로우/태스크 상태값", "DAG" — 전부 **동작 원리 설명**이 목적. Developer Guides(실무 활용법)나 Reference(룩업)와 명확히 다른 역할 |

### 1-3. 핵심적으로 가져올 수 있는 원칙 3가지

1. **콘텐츠 유형(개념/허브/레퍼런스)과 파일 경계가 일치한다** — 한 파일 안에서 유형을 섞지
   않는다. 섞일 때는(worker-task처럼) 개념은 앞쪽에 짧게, 레퍼런스는 뒤쪽에 표로, 라는 순서를
   지킨다.
2. **카테고리 진입 지점에 "허브"가 있다** — 단순 링크 목록(Docusaurus의 `generated-index`)이
   아니라, 왜 이 하위 문서들이 이 순서/그룹으로 있는지 한 줄씩 붙은 안내가 있다.
3. **페이지 끝에 다음 행동을 위한 링크가 있다** — prev/next, 관련 문서.

---

## 2. rag-docs 현재 상태 진단

### 2-1. `overview/` (개요)

| 파일 | 문제 |
|---|---|
| `01-product.md` | 두 성격이 한 파일에 섞임: (a) 제품 포지셔닝/경쟁사 비교(Dify·RAGFlow·R2R) — 세일즈 자료 성격, "구조 이해·설치·운영"이라는 문서 목적과 결이 다르고 경쟁사 기능 변경 시 별도 추적 부담 발생 (b) "제품 구성"/"인프라 스택" 문단이 `03-architecture.md` 1장과 상당 부분 중복 |
| `02-features.md` | "핵심 기능"이라는 이름이지만 실제로는 8개 영역 전체를 표로 나열한 **완전한 레퍼런스 카탈로그**(133줄, orkes 기준의 Reference Docs 성격). 처음 온 독자가 감을 잡는 개요 역할과, 이미 아는 사람이 필드를 찾아보는 레퍼런스 역할이 충돌 — orkes의 core-concepts(산문 설명 중심, 나열 최소화)와 대비됨 |
| `03-architecture.md` | 내용 자체는 우수하나, 1~2장(개념: 전체구성/데이터흐름)과 3장·6장(레퍼런스: 통신 매트릭스, HA 상태표)이 한 파일에 섞여 있음. orkes라면 전자는 Conceptual Guides, 후자는 Reference Docs로 분리했을 성격 |

### 2-2. `install/` (설치)

전반적으로 양호 — orkes의 "Quickstarts" 여정형 페이지와 성격이 가장 유사하게 이미 절차형으로
잘 구성되어 있다(요구사항 → quickstart → k8s → enterprise → 검증 → 연동).

| 파일 | 문제 |
|---|---|
| `06-integrations.md` | 제목이 "LibreChat 연동"으로 특정 벤더에 고정. 향후 다른 연동(Slack, Teams 등) 추가 시 파일명/제목이 안 맞게 됨. 지금 손대지 않아도 되지만 다음 연동 추가 시점에 구조가 흔들릴 위험 |

### 2-3. `operations/` (운영)

| 파일 | 문제 |
|---|---|
| `03-runbook-docker-compose.md` (587줄) | 최소 4개 성격이 한 파일에 혼재: (0)빠른 참조 cheat sheet, (1)~(3)절차형 runbook(헬스체크/장애대응/일상운영), (4)~(5)배포·초기설정 절차, (6)"컴포넌트 참조"(컨테이너 구성/이벤트 흐름/오브젝트 스토리지 레이아웃 — 이건 절차가 아니라 **아키텍처 레퍼런스**). "Redis 큐가 쌓일 때 뭘 해야 하지"를 찾으려면 587줄을 스크롤해야 함 |
| `01-runbook-k8s.md` (196줄) vs `03-runbook-docker-compose.md` (587줄) | 같은 "runbook" 목적인데 분량·구성 방식이 3배 차이 — 일관성 부족. k8s 쪽은 없는 "컴포넌트 참조" 섹션이 compose 쪽에만 있음 |
| `03장 6절 "컴포넌트 참조"` | `overview/03-architecture.md`의 통신 매트릭스·구성도와 내용이 겹칠 가능성 (이벤트 처리 흐름, 오브젝트 스토리지 레이아웃) |

### 2-4. `reference/` (레퍼런스)

| 파일 | 문제 |
|---|---|
| `01-api-guide.md` (1357줄) | 11개 리소스 영역(KB/멤버십/소유권/사용자/문서/재인덱싱/삭제/목록조회/커넥터/집계/검색/MCP)을 한 파일에 순서대로 나열. orkes의 레퍼런스는 **태스크 타입 1개 = 파일 1개**(`worker-task`, 필요시 다른 system task도 별도 파일)로 잘게 쪼개 링크 공유·검색엔진 인덱싱·부분 수정이 쉬움. api-guide는 앵커(`#3-지식베이스-kb`)로만 탐색 가능해 딥링크 공유나 diff 리뷰가 불리함 |
| `02-settings-guide.md`(659줄), `03-environment-guide.md`(160줄) | 분량·구성 적절 — 유지 |

### 2-5. `support/` (지원)

| 파일 | 문제 |
|---|---|
| `01-support-policy.md` | 제목에 "v1 (초안)"이 그대로 노출됨. 내용 완성도와 무관하게 퍼블릭 문서에 "초안" 라벨이 남아있는 것 자체가 고객 신뢰도 문제 |

---

## 3. 개선 방향 — 카테고리별 (틀 유지, 내용만 재배치)

### 3-1. `overview/`

- **`01-product.md`**: "제품 포지셔닝"(경쟁사 비교 다이어그램·표)을 별도 절로 명확히 분리하거나
  최소화하고, "제품 구성"/"인프라 스택" 문단은 요약 3~4줄 + `03-architecture.md` 링크로 축소
  (중복 제거, single source는 architecture 쪽에 둔다).
- **`02-features.md`**: 개요 카테고리에는 **영역별 1~2문장 요약 + 대표 기능 3~5개**만 남기고,
  현재의 완전한 표 카탈로그(8개 영역 전체)는 `reference/`로 옮기거나(예:
  `reference/04-features-catalog.md`) 최소한 이 파일 안에서 "요약 섹션"과 "전체 카탈로그
  섹션"을 명확히 분리한다. — orkes core-concepts처럼 개요는 산문 설명 비중을 높인다.
- **`03-architecture.md`**: 1~2장(전체 구성, 데이터 흐름 — 개념)과 3장·6장(통신 매트릭스,
  HA 상태표 — 레퍼런스)의 경계를 헤딩 레벨이나 콜아웃으로 시각적으로 구분한다. 파일을
  쪼개지 않더라도 "이 아래부터는 배포 설계용 참조표" 같은 안내 문구를 추가.

### 3-2. `install/`

- 현재 구조 유지. `06-integrations.md`만 제목을 "통합 연동"류로 일반화하고 내부에
  "1. LibreChat" 절 형태로 바꿔두면, 다음 연동 추가 시 같은 파일 안에 절만 추가하면 되어
  프레임이 흔들리지 않는다.

### 3-3. `operations/`

- **`03-runbook-docker-compose.md`**: 587줄 중 "6. 컴포넌트 참조"(컨테이너 구성/이벤트
  처리 흐름/오브젝트 스토리지 레이아웃)를 `overview/03-architecture.md`로 이관하거나
  `reference/`에 별도 파일로 분리. 남은 0~5장(빠른 참조/헬스체크/장애대응/일상운영/배포/초기설정)은
  전부 "지금 이 순간 뭘 해야 하는가"형 절차이므로 runbook 본연의 역할에 집중시킨다.
- **`01-runbook-k8s.md`**와 형식(장 구성, "빠른 참조" 섹션 유무)을 맞춰 두 runbook의
  탐색 경험을 통일한다.

### 3-4. `reference/`

- **`01-api-guide.md`**: 리소스 단위로 분리 검토(예: `01-api-kb.md`, `02-api-documents.md`,
  `03-api-connectors.md`, `04-api-search-mcp.md` 등, 번호는 예시). 파일이 늘어나되
  `reference/` 카테고리 자체는 그대로이므로 "틀"은 안 바뀐다. 분리가 부담되면 최소한
  각 리소스 섹션 시작에 앵커 링크를 문서 상단 목차로 모아 스캔성을 높인다.
- 인증/권한 개념 설명(1장)은 유지하되, orkes worker-task처럼 "개념은 짧게 앞에, 표는 뒤에"
  원칙을 지키고 있는지 다른 리소스 섹션에도 동일하게 적용되어 있는지 점검.

### 3-5. `support/`

- `01-support-policy.md` 제목에서 "v1 (초안)" 제거 — 실제 정책이 확정되지 않았다면
  퍼블릭 노출 대신 내부 검토 단계로 되돌리고, 확정되면 라벨만 뗀다.

---

## 4. 페이지 레벨 공통 템플릿 (orkes 패턴 응용)

폴더 구조는 여정 단계 기준(개요→설치→운영→레퍼런스→지원)으로 유지하되, **파일 하나하나는
아래 4가지 템플릿 중 하나로 성격을 명확히 하고 섞지 않는다.**

| 템플릿 | 적용 대상 예 | 구조 |
|---|---|---|
| 개념형 | `overview/01-product.md`, `03-architecture.md` 1~2장 | 1문단 요약 → 핵심 개념 불릿 → H2/H3 산문 설명, 표는 보조적으로만. 코드블록 최소화 |
| 허브형(신규) | 각 카테고리 최상단 — 현재 `_category_.json`의 `generated-index`가 단순 링크 목록만 보여줌 | "이 카테고리에서 무엇을 하게 되는지" 2~3줄 + 하위 문서를 "제목+한줄설명"으로 재소개. `_category_.json`의 `description`을 좀 더 길게 쓰거나, 카테고리 첫 문서(`01-*.md`)가 이 역할을 겸하게 한다 |
| 절차형(runbook/설치) | `install/*`, `operations/*runbook*` | 전제조건 → 번호 매긴 단계 → 각 단계 명령어/기대 결과 → 판정 기준. 지금 install 쪽이 이미 이 패턴에 가까움 — 유지, operations에도 동일 원칙 적용 |
| 레퍼런스형 | `reference/*`, 향후 분리될 features 카탈로그 | 짧은 개념 도입(1~2문장) → 범주별 표 → 코드 예제 → 관련 문서 링크. 산문 설명 최소화, 스캔 우선 |

문서 끝 "관련 문서" 링크는 이미 일부 파일에 인라인으로 있으나, 일관되게 각 파일 마지막에
"다음으로 볼 문서" 1~3개를 명시하는 관행을 표준화하면 orkes의 prev/next 경험에 가까워진다
(Docusaurus 자동 prev/next 네비게이션은 이미 활성화되어 있어 이 부분은 별도 구현 불필요 —
콘텐츠 관행만 맞추면 됨).

---

## 5. 실행 우선순위

| 우선순위 | 항목 | 이유 |
|---|---|---|
| P0 | `support/01-support-policy.md` "v1 (초안)" 라벨 정리 | 고객 대면 리스크, 작업량 최소 |
| P0 | `overview/01-product.md` ↔ `03-architecture.md` 중복(제품구성/인프라스택) 정리 | 정보 불일치 위험(한쪽만 갱신되면 어긋남) |
| P1 | `overview/02-features.md` 요약/카탈로그 분리 | 신규 독자 온보딩 체감 개선 |
| P1 | `operations/03-runbook-docker-compose.md` "6. 컴포넌트 참조" 이관 | 587줄 runbook의 탐색성 개선 |
| P2 | `reference/01-api-guide.md` 리소스 단위 분리 | 작업량 큼, 급하지 않음(현재도 앵커로 탐색 가능) |
| P2 | 카테고리 허브형 도입부 강화(5개 카테고리 전체) | 효과는 있으나 신규 콘텐츠 작성이 아니라 다듬기라 후순위 |

---

## 6. 하지 않을 것 (명시적 비범위)

- 사이드바 카테고리 추가/삭제/이름변경, `position` 재배열
- orkes의 카테고리 이름(Getting Started/Conceptual Guides 등)을 rag-docs에 그대로 붙이기
- orkes 전체 사이트 구조를 기준으로 한 전면 재설계 — 표본 4개 페이지에서 관찰된 원칙만 반영
- `docs/demo` 관련 변경 (별도 재작업 예정)
